{
	"private": true,
	"license": "MIT",
	"version": "0.1.0",
	"devDependencies": {
		"lerna": "^2.5.1"
	},
	"workspaces": [
		"packages/*"
	],
	"scripts": {
		"build": "lerna run build --ignore hydrokit-examples",
		"example": "yarn --cwd packages/hydrokit-examples start"
	}
}
